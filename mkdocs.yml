site_name: XiCorPy

theme:
  name: "material"

markdown_extensions:
  - footnotes

plugins:
  - search
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          rendering:
            show_source: false
      watch:
        - src

nav:
  - Home: index.md
  - Chatterjee's Xi: xi.md
  - Conditional Dependence: codec.md
  - API Reference:
      - XiCor: xicor_api.md
      - Conditional Dependence: codec_api.md
      - FOCI: foci.md
